# Write your MySQL query statement below
select customer_id,COUNT(v.customer_id) as count_no_trans FROM visits v LEFT JOIN  transactions t On v.visit_id = t.visit_id WHERE t.transaction_id is null group by v.customer_id;